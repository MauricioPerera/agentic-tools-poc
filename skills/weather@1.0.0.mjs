var p=async(i,o)=>{let{latitude:e,longitude:t}=i;if(typeof e!="number"||typeof t!="number")throw new Error("latitude and longitude must be numbers");if(e<-90||e>90)throw new Error(`latitude ${e} out of range (-90 to 90)`);if(t<-180||t>180)throw new Error(`longitude ${t} out of range (-180 to 180)`);let a=`https://api.open-meteo.com/v1/forecast?${new URLSearchParams({latitude:String(e),longitude:String(t),current:"temperature_2m,wind_speed_10m,weather_code",daily:"temperature_2m_max,temperature_2m_min",timezone:"auto",forecast_days:"1"}).toString()}`;o.log(`GET ${a}`);let r=await o.fetch(a);if(!r.ok)throw new Error(`open-meteo returned ${r.status}`);let m=await r.json(),n=m.current??{},u=m.daily??{};return{location:`${e},${t}`,temp_c:n.temperature_2m??0,wind_kph:n.wind_speed_10m??0,weather_code:n.weather_code??0,forecast_max_c:u.temperature_2m_max?.[0]??0,forecast_min_c:u.temperature_2m_min?.[0]??0}},_=p;export{_ as default};
