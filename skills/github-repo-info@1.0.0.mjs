var s=async(e,o)=>{if(!e.owner||!e.repo)throw new Error("owner and repo are both required");let n=`https://api.github.com/repos/${encodeURIComponent(e.owner)}/${encodeURIComponent(e.repo)}`;o.log(`GET ${n}`);let a={Accept:"application/vnd.github+json","User-Agent":"agentic-tools-poc"};o.env.GITHUB_TOKEN&&(a.Authorization=`Bearer ${o.env.GITHUB_TOKEN}`);let r=await o.fetch(n,{headers:a});if(r.status===404)throw new Error(`repo ${e.owner}/${e.repo} not found (or private without auth)`);if(r.status===403)throw new Error("GitHub API rate-limited (60 req/h unauth) \u2014 set GITHUB_TOKEN to raise to 5000 req/h");if(!r.ok)throw new Error(`github returned ${r.status}`);let t=await r.json();return{full_name:t.full_name??`${e.owner}/${e.repo}`,description:t.description??"",stars:t.stargazers_count??0,language:t.language??"",default_branch:t.default_branch??"main",pushed_at:t.pushed_at??"",url:t.html_url??`https://github.com/${e.owner}/${e.repo}`}},i=s;export{i as default};
