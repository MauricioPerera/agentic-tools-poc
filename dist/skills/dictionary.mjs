var d=async(r,o)=>{let t=String(r.word??"").trim().toLowerCase();if(!t)throw new Error("word is required");if(/\s/.test(t))throw new Error(`"${r.word}" looks like a phrase \u2014 pass a single word`);let s=`https://api.dictionaryapi.dev/api/v2/entries/en/${encodeURIComponent(t)}`;o.log(`GET ${s}`);let n=await o.fetch(s);if(n.status===404)throw new Error(`no dictionary entry for "${t}"`);if(!n.ok)throw new Error(`dictionaryapi.dev returned ${n.status}`);let i=(await n.json())[0];if(!i)throw new Error(`empty response for "${t}"`);let a=i.phonetic?.trim()||i.phonetics?.find(e=>e.text?.trim())?.text?.trim()||"",p=(i.meanings??[]).map(e=>({partOfSpeech:e.partOfSpeech??"",definition:e.definitions?.[0]?.definition??"",example:e.definitions?.find(c=>c.example?.trim())?.example?.trim()??""}));return{word:i.word??t,phonetic:a,meanings:p}},h=d;export{h as default};
